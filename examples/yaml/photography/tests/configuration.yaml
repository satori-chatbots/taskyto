# Default values unless overridden by test-specific values
default:
  match-strategy:
    tolerance: 0.0
    use-llm: false

tests:
  - name: 'price_estimation.yaml'
    match-strategy:
      tolerance: 0.3

  - name: 'schedule_appointment.yaml'
    match-strategy:
      tolerance: 0.41